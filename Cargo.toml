[package]
name = "qmk-herdr"
version = "0.1.0"
edition = "2021"
description = "Mirror Herdr agent state on a QMK keyboard over USB MIDI"

[dependencies]
midir = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
